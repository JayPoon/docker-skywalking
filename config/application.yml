naming:
  jetty:
    host: ${NAMING_BIND_HOST:localhost}
    port: ${NAMING_BIND_PORT:10800}
    context_path: /
remote:
  gRPC:
    host: localhost
    port: 11800
agent_gRPC:
  gRPC:
    host: localhost
    port: 11800
agent_jetty:
  jetty:
    host: localhost
    port: 12800
    context_path: /
analysis_register:
  default:
analysis_jvm:
  default:
analysis_segment_parser:
  default:
    buffer_file_path: ../buffer/
    buffer_offset_max_file_size: 10M
    buffer_segment_max_file_size: 500M
ui:
  jetty:
    host: localhost
    port: 12800
    context_path: /
# storage:
#   elasticsearch:
#     cluster_name: docker-es-cluster
#     cluster_transport_sniffer: true
#     cluster_nodes: 172.20.55.83:9300
#     index_shards_number: 2
#     index_replicas_number: 0
#     ttl: 7